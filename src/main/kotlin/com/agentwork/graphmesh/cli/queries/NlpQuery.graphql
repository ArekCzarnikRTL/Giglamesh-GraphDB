query NlpQuery($input: NlpQueryInput!) {
  nlpQuery(input: $input) {
    answer
    wasReformulated
    effectiveQuestion
    durationMs
    sources
    detectedIntent {
      intent
      confidence
      reasoning
    }
  }
}
